#include "main.h"

/**
 * _isupper - define if a character is uppercase
 * isupper - checks if alphabet is ippercase
 * @c: charater to check
 * Return: void
 */

int _isupper(int c)
{
	if (isupper(c))
		return (1);
	else
		return (0);
}
